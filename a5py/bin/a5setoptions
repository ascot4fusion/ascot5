#!/usr/bin/env python

import a5py.ascot5io.options as options

import sys
sys.path.append('.')
import opt

if len(sys.argv) > 1:
    fn = sys.argv[1]
else:
    fn = "ascot.h5"

o = opt.generateopt()
options.write_hdf5(fn, o)
print("Options set.")
