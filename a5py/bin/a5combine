#!/usr/bin/env python

import sys
from a5py.ascot5io.tools import combine

print("Combining files\n")
for fn in sys.argv[2:]:
    print(fn)

print("\nwith " + sys.argv[1] +"\n")

fnt = sys.argv[1]
fns = sys.argv[2:]

combine(fnt, fns, mode="add")
