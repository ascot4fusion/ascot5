#!/usr/bin/env python

import sys
import a5py.ascot5io.tools as tools

if len(sys.argv) > 2:
    print('Removing run ' + sys.argv[2] + ' from ' + sys.argv[1])
    tools.removerun(sys.argv[1], sys.argv[2])
else:
    print('Removing all runs from ' + sys.argv[1])
    tools.removerun(sys.argv[1])
