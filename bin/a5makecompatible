#!/usr/bin/env python3

import sys
import os

import a5py.ascot5io.compatibility as comp

if len(sys.argv) != 2:
        print("Convert HDF5 file of the previous version to current version.")
        quit()

fnin  = sys.argv[1]

comp.convert(fnin)
